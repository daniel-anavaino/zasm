Uses the arrow keys and EDIT to move around and to enter/exit edit mode (respectively) but the comments haven't been updated to reflec that.
Doesn't ignore "bad" keys.
Next part is to start handling assembly source editing.
